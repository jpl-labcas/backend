<?xml version="1.0" encoding="UTF-8"?>
<pgeConfig>

	<!-- PGE commands -->
	<exe dir="[JobDir]" shell="/bin/bash">
		<cmd>mkdir -p output</cmd>
		<cmd>echo 'xyz' > output/gene.counts</cmd>
	</exe>

	<customMetadata>
		<metadata key="ProductionDateTime" val="[DATE.UTC]" />
		<metadata key="JobDir" val="[PGE_ROOT]/rnaseq-pipeline/jobs/[sample_id]" />
	</customMetadata>

	<!-- CAS Crawler directives -->
	<output>
		<dir path="[JobDir]/output" createBeforeExe="false">
			<!-- metadata extractors matching specific files -->
			<files regExp=".*gene\.counts"
				metFileWriterClass="org.apache.oodt.cas.pge.writers.metlist.MetadataListPcsMetFileWriter"
				args="[LABCAS_HOME]/workflows/rnaseq-pipeline/pge-configs/metout.xml" />
		</dir>
	</output>

</pgeConfig>